# pysnakey
Another clone of the snake-like game, written in, well, Python. 

### Dependencies
    sudo pip install -r requirements.txt

### Controls
The directional keys (arrows) move you, the snake, around and ***Q*** or escape leaves the game.
